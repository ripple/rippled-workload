[project]
    name = "workload"
    version = "0.1.0"
    description = "Add your description here"
    authors = [
        { name = "Michael Legleux", email = "legleux@gmail.com" }
    ]
    dependencies = [
    "xrpl-py",
    "requests",
]
    readme = "README.md"
    requires-python = ">= 3.11"
    license = { text = "MIT" }

[project.scripts]
    "workload" = "workload.app:run"

[build-system]
    requires = ["hatchling"]
    build-backend = "hatchling.build"

[tool.rye]
    managed = true
    dev-dependencies = [
        "ruff"
    ]

[tool.hatch.metadata]
    allow-direct-references = true

[tool.hatch.build.targets.wheel]
    packages = [
        "src/workload",
    ]
